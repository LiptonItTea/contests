#include <iostream>

using namespace std;

int main(){
    long long int t;
    int k;
    cin >> t >> k;

    int l = 0;
    int r = t;
    bool* results = new bool[t];
    for (int i = 0; i < t; i++)
        results[i] = false;

    int m = (l + r) / 2;
    while(results[m - 1] == results[m]){
        int c = 
    }
}