#include <iostream>
#include <math.h>
#include <map>
#include <set>
#include <iomanip>
#include <vector>
#include <climits>
#include <string>
#include <algorithm>
#include <queue>
#include <list>
#include <stack>

#define ll long long

using namespace std;

struct MonDeq {
    deque<int> deq;

    void add(int i) {
        
    }
};

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cout << fixed << setprecision(9);




    // int n, m, a, b; ll seed, x, y;
    // cin >> n >> m >> a >> b >> seed >> x >> y;
    //
    // vector<vector<ll>> heights (n, vector<ll> (m));
    // ll s0 = seed;
    // for (int i = 0; i < n; i++) {
    //     for (int j = 0; j < m; j++) {
    //         s0 = (s0 * x + y) % 1000000000;
    //         heights[i][j] = s0;
    //     }
    // }
}